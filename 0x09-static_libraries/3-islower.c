#include "main.h"

/**
 * _islower - is a function which checks for lowercase character
 * @c: represents the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
